# Flask web framework and extensions
Flask>=2.3.0
Werkzeug>=2.3.0

# HTTP requests
requests>=2.31.0

# Environment variable management
python-dotenv>=1.0.0

Pillow

# ─── RxGuard: Elastic core + AWS reasoning (hackathon additions) ─────────────

# Elasticsearch is the retrieval core: hybrid BM25 + kNN over the CDSCO corpus,
# plus the append-only audit index. Pinned below 9 to match the 8.17 cluster.
elasticsearch>=8.17,<9

# AWS SDK — Bedrock Converse for generation, Titan v2 for embeddings.
# Present unconditionally so the production path is one credential away.
boto3>=1.34.0

# Local ONNX embeddings (BAAI/bge-small-en-v1.5, 384-dim). Free and offline,
# so vector search works without paid embedding calls. onnxruntime-backed —
# deliberately not sentence-transformers, which would drag in ~2GB of torch.
fastembed>=0.4.0

# Gazette PDF text extraction for the ingestion pipeline.
pymupdf>=1.24.0

# Production WSGI server. The Flask development server is single-threaded and
# prints a warning on every boot; with N*N medication screens issuing many
# sequential LLM calls, concurrent workers matter for a live demo.
gunicorn>=21.2.0
